const {NetworkClient} = require('@frondjs/frond')

module.exports = function registerNetworkClients() {
  /*
  new NetworkClient({
    id: 'someapi',
    userCache: {lifetime: 0},
    url: 'https://clientapi.gozel.com.tr',
    method: 'GET',
    headers: {
      'Content-Type': 'application/json'
    },
    redirect: 'follow',
    mode: 'cors',
    credentials: 'omit'
  })
  */
}
